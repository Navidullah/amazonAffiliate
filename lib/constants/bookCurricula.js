// Top-level curriculum buckets for /books. `Book.curriculum` is a Mongoose
// enum (see lib/models/Book.js), so any new value added here must also be
// added to that enum.
export const BOOK_CURRICULA = ["FBISE", "IB", "IGCSE", "GCSE"];
